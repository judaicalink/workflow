# Workflow for JudaicaLink

This repository contains the Python scripts to run the Compact Memory and JudaicaLink workflow.
They are optimized run fully automatically and can be run as Django commands.
They do automatic processing of the XML and TXT files, OCR improvement, NLP Pipeline and build the SOLR CORES.
